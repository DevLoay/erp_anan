services:
  - type: web
    name: mohamed-shawki-erp
    runtime: node
    plan: free
    region: frankfurt
    buildCommand: npm ci && npm run build
    startCommand: npx prisma migrate deploy && node scripts/ensure-admin-user.mjs --apply --confirm=ENSURE_ADMIN_USER && npx next start --port $PORT
    healthCheckPath: /api/health
    autoDeployTrigger: commit
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: mohamed-shawki-erp-db
          property: connectionString
      - key: AUTH_SECRET
        generateValue: true
      - key: NEXTAUTH_URL
        value: https://mohamed-shawki-erp.onrender.com
      - key: APP_URL
        value: https://mohamed-shawki-erp.onrender.com
      - key: AUTH_DEBUG
        value: "1"
      - key: ADMIN_EMAIL
        value: admin@logistics-erp.com
      - key: ADMIN_PASSWORD
        value: Admin@123456
      - key: NEXT_PUBLIC_APP_NAME
        value: MOHAMED SHAWKI ERP
      - key: UPLOAD_DIR
        value: public/uploads

databases:
  - name: mohamed-shawki-erp-db
    databaseName: mohamed_shawki_erp
    user: erp_user
    region: frankfurt
    plan: free
    ipAllowList: []
